
## 项目代码结构

```
Bioscope
├── apply                       # 应用层
│   └── GUI_bioscope.py         # UI功能类
│   └── ui_mainwindow.py        # UI原始界面类
│   └── taskwork.py             # 扫描任务分发类
├── control                     # 控制层
│   └── core.py                 # 控制显微镜的核心代码
│   └── processing_handler.py   # 配置控制参数
│   └── utils.py               
│   └── utils_config.py         
├── DataSaver                   # 数据存储类
│   └── data.py                 # 控制显微镜的核心代码
│   └── DataExplorer.py                 # 控制显微镜的核心代码
│   └── Graph.py                 # 控制显微镜的核心代码
│   └── Saverdata.py                 # 控制显微镜的核心代码
├── Drives                      # 数据存储类
│   └── gxipy                 # 控制显微镜的核心代码
│   └── def.py                 # 控制显微镜的核心代码
│   └── camera.py                 # 控制显微镜的核心代码
│   └── loadercontroller.py                 # 控制显微镜的核心代码
│   └── microcontroller.py                 # 控制显微镜的核心代码
├── processing                     # 程序入口
│   └── loadercontroller.py                 # 控制显微镜的核心代码
│   └── microcontroller.py                 # 控制显微镜的核心代码
├── utils
│   └── loadercontroller.py                 # 控制显微镜的核心代码
│   └── microcontroller.py                 # 控制显微镜的核心代码
│   └── loadercontroller.py                 # 控制显微镜的核心代码
│   └── microcontroller.py                 # 控制显微镜的核心代码
│   └── loadercontroller.py                 # 控制显微镜的核心代码
│   └── microcontroller.py                 # 控制显微镜的核心代码
├── channel_configurations.xml                  # 扫描参数配置文件
├── configuration_octopi.txt    # 位移台参数配置文件
├── config.yaml  # 相机光源参数配置文件
├── configuration_octopi.ini  # 相机光源参数配置文件
├── main.py  # 相机光源参数配置文件
```
